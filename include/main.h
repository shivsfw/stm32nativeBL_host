#ifndef MAIN_H
#define MAIN_H

using namespace std;


#include <Windows.h>
#include <cstdio>
#include <vector>
#include <algorithm>
#include <iostream>
#include <conio.h>
#include <cstring>
#include <stdint.h>

// #include "configure.h"
// #include "windowsSerial.h"
// #include "windowsCAN.h"
// #include "ui.h"
// #include "Filecommands.h"

//Pending features:
// 1. Save the message in response to commands in global buffers to read them back.
// 2. Add UART command code if needed to test.
// 3. Read the memory sector by sector and write into a file to compare later(as the read command is restricted to read only 255 bytes in one go).
// 4. Erase the memory sector by sector and confirm by reading few first pages. 
// 5. Write to the memory and read the same memory location to compare it with the sent file. 
// 6. Go to the application and start running the application 
// 7. Change the speed to transfer teh data even faster.
// 8. Check for ACK and NACK from BL.

#endif